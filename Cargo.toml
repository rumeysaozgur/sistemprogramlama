[package]
name = "aitoolgrep"
version = "0.1.0"
edition = "2021"
description = "AI-friendly parallel search, replace, and codebase statistics tool"
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
eframe = "0.31"
rayon = "1.10"
regex = "1.11"
rfd = "0.15"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
walkdir = "2.5"

[dev-dependencies]
tempfile = "3.15"

[profile.release]
lto = true
codegen-units = 1
strip = true
